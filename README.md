# Vehicle-Insurance-Estimator-Project-
this is a full stack application which allows users to input specific information and then outputs an estimate. The Data is then pushed into a Oracle SQL server using PL/SQL and JDBC. The server includes PL/SQL objects and a fully relational database.
